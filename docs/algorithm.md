# アルゴリズムと保証範囲

[English](en/algorithm.md) | 日本語

## 対象

BACは、矩形footprintを持つ差動二輪・Ackermann・全方向ロボットについて、robot frameの障害物点、robot
frameへ変換した局所path、現在の並進・角速度から、次の定曲率指令`(v,w)`を選ぶ。障害物は1制御周期内
では静的点群とする。どちらのモデルも車体は定曲率で運動するものとして扱う。差動二輪は並進候補だけを
scoring上の下限`turn_radius_min`で縛り、その場旋回は自由である。Ackermannは車体曲率そのものを
運動学制約として縛り、その場旋回を持たない。nav2 MPPIと同じく、車両モデルの記述はこの車体レベルの
制約までとし、road-wheel kinematicsは下位controllerの責務とする。

![候補円弧と左右クリアランス](images/bac_geometry.svg)

```mermaid
flowchart LR
  A[robot-frame obstacle points] --> D[filter / point cap]
  B[robot-frame local path] --> E[path station model]
  C[current velocity] --> F[motion model candidate policy]
  D --> G[emergency envelope]
  D --> H[bilateral arc evaluation]
  E --> I[candidate score]
  F --> H
  G -->|occupied| S[stop / observed escape]
  H --> J[stop-before-contact test]
  J --> I
  I --> K[best v, w + diagnostics]
```

## 処理

1. 入力点を距離と自己反射除外boxでfilterし、`max_points`を超える場合は入力順を保って間引く。
2. 現在速度の制動距離を含むfootprint周囲のemergency envelopeを調べる。移動中に点が入れば停止を
   優先し、停止済みならoffending pointから離れる低速escape候補だけを許す。後退escapeを有効にする
   場合は、後方観測を前提にできなければならない。
3. 現在の進行方向で衝突コースにある点から並進速度上限を計算する。平行壁のように現在円弧が余裕を
   持って外す点は、この減速判定から除外する。
4. 選択したmotion modelで候補を生成する。差動二輪は並進dynamic windowとヨーレートを組み合わせ、
   その場旋回も追加できる。Ackermannは後退を含む各速度について許容曲率
   `|kappa| <= min(1 / turn_radius_min, w_max / |v|)`をsampleし`w = v * kappa`へ変換する。
   その場旋回は候補格子に現れない。後退有効時はどちらも停止近傍の
   escape候補を追加できる。
5. 各候補について、定曲率運動中の矩形footprintと障害点の初接触弧長を閉形式で求める。接触前に
   設定した`stop_decel`で停止できない候補は棄却する。
6. 候補終端をpathへ射影し、進行度、経路接線との方位差、弱い横偏差を計算する。障害物で塞がれた
   path区間では横偏差の引力を無効化する。
7. 左右クリアランス、狭所での左右差、path項、前回操舵との差、側方圧迫を合成する。粗探索後は差動二輪
   ならヨーレート、Ackermannなら曲率の近傍を細分し、1周期到達可能範囲へ制限して円弧を再検証する。
   接触により追加減速する場合は`v`と`w`を同率で下げて曲率を保存し、必要なら到達可能性と接触を再検証する。

既定値での概略scoreは次のとおりである。`tightness`は両側が狭い場合だけ1へ近づく。

```text
score = 2.0 * min(clearance, adaptive_cap)
      - 4.0 * tightness * abs(clearance_left - clearance_right)
      - 1.0 * (remaining_path_arclength + 0.3 * path_offset)
      - 0.15 * abs(heading_error_vs_path_tangent)
      - 0.6 * steering_change
      - 0.5 * abs(v) * lateral_squeeze
```

低速時も最低`min_eval_distance`まで評価する。ただし遠方まで円弧を外挿して反対側の壁を誤評価しない
よう、評価角`eval_angle_max`と横変位`eval_lateral_max`で窓を制限する。`steering_change`は差動二輪では
ヨーレート差、Ackermannでは車体曲率差である。全パラメータと単位は[パラメータリファレンス](parameters.md)
を参照する。

## 主張を3段階に分ける

### 実装で固定している判定規則

- 観測点がemergency envelope内にある間、通常の前進候補を出さない。
- 観測した静的点との予測接触前に停止できない定曲率候補を採用しない。
- 衝突コース上の点に対する並進速度上限は、同じ判定条件内では近づくほど厳しくなる。
- 狭所では左右クリアランス差をscoreへ明示的に入れる。

これらは入力点、footprint、制動modelが正しいという条件下の**アルゴリズム上の規則**である。
センサ未観測領域や実機追従誤差を含む衝突回避保証ではない。

### シミュレーションで観測した性質

以下の公開済みbenchmark観測は差動二輪モデルによる。

- 正準18シナリオ × 3 runでBACは54/54成功、衝突0、最接近0.136 mだった。
- 1.5 m通路のpath横ずれ0.10〜0.25 mでは8/8成功し、平均到達時間28.8 s、クリアランスは
  0.225〜0.230 mだった。
- 1 runの開口幅sweepでは1.25 mまで完走したが、1.15 mでは最接近0.050 m、timeoutとなった。
  他の3 controllerは同じ1.15 m試行を完走しており、この境界条件でBACの優位性は観測されなかった。
- 全方向policyは、回避を横速度で行いヨーレートを姿勢規範に使う。候補格子は「前進速度 × 横速度」で、
  次元は差動二輪の「前進速度 × ヨーレート」と同じ2次元である。候補数は同数ではない。同梱の全方向設定の
  値（`limits.v_max` 0.4、`limits.vy_max` 0.3、`limits.w_max` 1.0、`v_samples` 5、`vy_samples` 15、
  `w_samples` 25）で現在速度 (0.20 m/s, 0.0 rad/s) から測るとomni 96・差動二輪130である。
  現在速度（前進成分）を0.00〜0.40 m/sの41点（0.01刻み、ヨー0、開けた場でpathは直進）で掃引すると、
  **同梱の `limits.v_min: 0.0` では差動二輪は130が21点・156が20点で、182は一度も出ない**。182が出るのは
  後退を許した場合だけで、`limits.v_min` = −0.05では130が1点・156が20点・182が20点、−0.10と−0.15では
  130が1点・156が31点・182が9点になる。omniは41点すべてで96である。ここで数えているのは粗格子だけで
  （`w_refine_steps` を0にして測った）。同梱既定の3では細分候補が2×3件加わり、どの数値も6大きくなる
  （差動二輪136×21点・162×20点、omni 102×41点）。細分は粗格子の勝者が停止行でないときに走るので、
  この+6は勝者が動く行である限り成り立つ。空の場でも1.2 m前方に壁を置いた場でも、現在速度0.00を含む
  全41点で成り立つことを実測した。
  R19 H3: ここには「同じ設定でも現在速度が変われば156や182になる」と書かれていたが、182は同梱設定では
  出得ない値で、直前の文が宣言する条件と食い違っていた。ヨーレートは候補生成の前に確定するため、
  採点し接触判定する軌道と実際に走る軌道が一致する。狭所では左右クリアランスの不均衡を姿勢規範へ加え、
  斜行ではなく機体を隙間へ向ける（斜行する矩形は直進より広く掃引するため。0.7 × 0.5 mの車体が55°で
  0.86 m、直進なら0.5 m）。この項は通路条件（両側がcap以内かつ正面が開いている）でのみ働く。
  capは`footprint.width / 2 + avoid_margin.side`である。同梱の全方向設定の車体幅0.5 mで
  `avoid_margin.side`を0.5にするとcapは0.75 mとなり、通常の1.6 m通路では遠い側が「開いている」と
  判定されてこの項は一度も発火しない。
  **測定条件を明記する**（R15 M12は、条件を書かない数値表がレビューアの手元で再現しないことを指摘した）。
  進入オフセット0.30 m、両モデルとも`avoid_margin.side = 0.9`、通路長7.5 m（x = 2.0〜9.5 m）、
  `heading_gain = 1.5`。
  この条件（壁厚0.10 m）での平均横偏差は、通路幅1.1 / 1.2 / 1.3 / 1.6 / 1.8 mに対し
  0.0131 / 0.0148 / 0.0171 / 0.0159 / 0.0160 mであり、同条件の差動二輪（0.0259 / 0.0280 / 0.0288 /
  0.0260 / 0.0255 m）に対して1.6〜2.0倍小さい。`avoid_margin.side`を下げると左右均衡項が発火せず、
  1.6 m通路（壁厚0.10 m）での平均横偏差は0.9で0.0159 m、0.5で0.1412 mになる。遷移は非単調である。
  0.500〜0.900を0.005刻みで掃引した実測（81点）は
  0.690=0.2009、0.695=0.1900、0.700=0.0330、0.705=0.1869、0.710=0.0152、0.715=0.0156、0.720=0.0150、
  **0.725=0.1600**、0.730=0.0156、0.735以降は0.0131〜0.0162である。
  **ただしこの81点から「0.730以上なら安全」と読んではならない。** 81点はいずれも再現するが、
  0.005格子はエイリアスしており、0.001刻み（0.730より上は0.0002刻み）で追試すると悪い点は点ではなく
  **帯**である。実測: 0.702〜0.708が0.1863〜0.1952（0.005格子は0.705しか踏まない）、0.721〜0.729が
  0.1444〜0.1600（この帯の中で良いのは0.723=0.0146の1点のみ。0.005格子は0.725しか踏まない）、
  **0.7370〜0.7386が0.0846〜0.1074**（ピークは0.7380・0.7382の0.1074。ただし帯の中にも良い点があり
  0.7376と0.7378は0.0160である。0.005格子も0.001格子もこの帯をほぼ踏み外す——0.735=0.0131、
  0.736=0.0169、0.739=0.0160）。したがって正しい言明は
  「**0.70〜0.74は0.005格子上でエイリアスする非単調帯であり、この範囲の値を測定点の補間で決めては
  ならない**（隣り合う1/1000で1桁変わる）」である。0.74より上は平坦で、0.7388〜0.900を0.0002刻みに
  掃引した807点は**すべて0.015658（`avoid_margin.side` = 0.8004 のとき）以上
  0.016315（同 0.8726）以下**、すなわち全点が0.017未満で、上の帯が達する0.08〜0.20には
  一つも近づかない。同種の帯は**見つからなかった**（0.0002という
  刻み幅での測定であり、それより狭い帯の不在は示せていない）。R19 4巡目: ここには
  「0.0157〜0.0163」と書かれていたが、807点のうち28点がその区間の外にあり成り立たない
  （粗い部分抽出の値がそのまま残っていた）。807点を全数測り直した。同梱の0.9はこの帯から十分離れており、
  0.8（0.016102）も0.9（0.015945）とほぼ同値である。つまり同梱の0.9はこの境界に対する余裕であって、
  必須の値ではない。R19 H4: ここには「0.705（0.1900）・0.710（0.1869）で
  戻り、0.715以降で安定」と書かれていたが、0.1900 / 0.1869は実際には0.695 / 0.705の値で1〜2格子ずれて
  おり、0.725という反例が抜けていた。R19の対応1巡目はこれを「0.017未満で安定するのは0.730以降」と
  直したが、それは0.005格子上の言明としてしか正しくない（0.737〜0.738が反例）。R18 M6: ここに
  書かれていた0.0119 / 0.139という対は、この段落が宣言する
  厚み0.10 mのrigではなく旧来の厚みゼロのrigの値だった。
  **進入オフセットには限界がある**。0.30〜0.50 mを0.002 m刻みで掃引した実測（101セル、接触・非完走・
  通路外への迂回のいずれかを不合格とする）では、厚み0.10 mの壁で幅1.2 mの通路は**全101セルが合格**、
  幅1.1 mでは0.418 mから不合格になる。厚みゼロの線分でモデル化するとどちらも悪化し、幅1.2 mで0.476 m、
  幅1.1 mで0.372 mから不合格になる。
  孤立障害物の迂回では`max|w|`が0.0437 rad/s（差動二輪は0.3125 rad/s）で、ヨーをほとんど使わずに横へ
  避ける。決定論的試験27件（単体10・閉ループ17）を通過する。
- Ackermann policyは、速度が加速度制限され曲率が有限速度で変化するplantに対する決定論的試験
  13件を通過する。前方側方goal、offset通路、障害物迂回、行き止まり停止、後方goal、旋回半径拘束、
  ヨーレート上限拘束、clearance probe到達距離、同梱設定例、安全停止（前進のみ）、
  安全停止（後退退避）、狭路centering、clutter走破である。
  後方goalの試験は、`turn_radius_min`以外を揃えた差動二輪の参照設定がその場旋回するのに対し、
  Ackermannは旋回しないことを対比して確認する。狭路centeringは横偏差・曲率符号反転・停止tickを、
  clutterはクリアランスと停止tickを閾値検査し、候補生成や履歴項を壊すと落ちる。1周期あたりの曲率
  変化は、閾値が正常時と破壊時を分離できると実測できたoffset通路と同梱設定走行で検査する。
  これは回帰試験であり、実車evidenceではない。

これは[評価条件](method_comparison.md#nav2-system-benchmark)内の観測であり、未試験環境へ
確率的・普遍的に一般化しない。

### 保証しないこと

- センサ死角、遅延、外れ値、誤った外部パラメータ下での安全
- 動的障害物の将来位置予測
- 地図、TF、plan、odomを含む任意の上流障害からの独立
- 角加速度・操舵過渡中のswept trajectoryとjerk
- 滑り、下位速度制御遅れ、制御周期超過を含むstop-before-contact
- 車体twistからroad-wheel操舵への下位変換と、その操舵速度・追従誤差
- `diff_drive`・`ackermann`でのgoal姿勢指定。ヨーで操舵するモデルは方位を進行方向と独立に選べないため、
  終端姿勢は経路接線が残した向きになる。全方向モデルはgoal姿勢に従う
- **薄い壁に沿って、ほぼ平行に進入する場合の接触回避。** BACは与えられた点群しか見ない。厚みの無い
  面に対してほぼ接線方向からレイを撃つと、ビーム間隔より薄い見かけの断面は隙間をすり抜けるため、
  車体が触れようとしている縁が点として現れないことがある。測れないものは避けられない。角度分解能
  0.5°（360°で720本）で厚み0の壁面に沿って進入した場合、`616746b`では接触が実測された。**現行revisionでも
  解消していない。** 幅1.2 m・厚みゼロの通路を進入オフセット0.30〜0.50 mで0.002 m刻みに掃引すると、
  **0.478 m・0.488 m・0.498 mの3セル**で車体接触が残る（R18 H5。以前ここには「その位置へ入らなく
  なった」と書いていたが偽である。R19 M3: 接触数を2と書いていたのも過小で、`runOmni` の `collided`
  で数え直すと3である）。厚み0.10 mの同じ通路は全101セルが接触なしで完走する。実際の壁は面を持つため、
  厚みゼロは評価上の縮退であり、回帰試験は厚みのある壁で行う。分解能を上げる（2880本以上）か、壁に
  厚みを与えるといずれも解消する。これはセンサ側の限界であって、controller側の限界ではない
  （R19 L11: 英語版のみに残っていた同趣旨の一文を日本語版へ戻した）。
  0.478のセルは浮動小数の表現に敏感である。掃引を素直に `start + step * i` で組んだ
  `0.30f + 0.002f * 89` は0.478000015となり接触する（最接近0.2874 m）が、リテラル `0.478f` は
  0.477999985で接触しない（最接近0.5448 m）。差は3e-8であり、掃引をこの形で組む限り接触する。
  なお4条件（幅1.1・1.2 m × 厚み0.10 m・厚みゼロ）計404セルの不合格は**91件**で、うち**接触は3件**である
  （すべて厚みゼロの1.2 m通路）。**迂回と非完走の切り分けは判定基準に依存する**（R19 L9。基準が
  どこにも定義されていなかった）ので、ここでは基準を明記する——接触は走査点が車体矩形へ入ったこと
  （閉ループの `collided`）、迂回は通路中心からの横偏差が「通路幅/2 + 壁厚」を超えたこと、非完走は
  そのいずれでもなく最終 x が 9.0 m に届かないこと、優先順は接触→迂回→非完走、**横偏差は走行全体で取る**。
  走行全体で取った実測は1.2 m/0.10 mが0件、1.1 m/0.10 mが35件（迂回34・非完走1、最初の不合格0.418）、
  1.2 m/厚みゼロが10件（接触3・迂回3・非完走4、最初0.476）、1.1 m/厚みゼロが46件（迂回46、最初0.372）で、
  合計は接触3・迂回83・非完走5である。**迂回と非完走の配分は、計測窓の中では2つの閾値のどちらでも
  同じで、窓を外すと閾値で変わる。** 同じデータで横偏差を通路内（計測窓 x = 3.0〜9.0 m）に限ると
  **迂回72・非完走16**になり、しかもこの窓の中では閾値を「通路幅/2 + 壁厚」（4条件でそれぞれ
  0.70 / 0.65 / 0.60 / 0.55 m）にしても `testNarrowCorridorCentering` が使う
  `kInsideCorridor` = 0.35 にしても**4条件すべてで同じ72・16**になる（実測）。窓を外すと
  閾値が効き、「通路幅/2 + 壁厚」では上のとおり迂回83・非完走5、0.35では迂回324・非完走0になる。
  以上が測った4セルである。R19 M3の対応1巡目が書いた迂回72・非完走16は、この窓つきの基準——
  `testNarrowCorridorCentering` と同じ基準——で厳密に再現する。**1巡目の値は誤りではなく、
  基準が書かれていなかっただけである**（R19検証で判明）。
  測った4基準のうち、総数91・各条件の不合格件数（0 / 35 / 10 / 46）・最初の不合格位置
  （− / 0.418 / 0.476 / 0.372）が一致するのは、走行全体+「通路幅/2 + 壁厚」・窓+「通路幅/2 + 壁厚」・
  窓+0.35 の3基準である。**走行全体+0.35 では一致しない**——総数327、各条件85 / 82 / 80 / 80、
  最初の不合格位置0.332 / 0.338 / 0.340 / 0.342 になる（迂回324・非完走0は上に書いたとおり）。
  接触3件は測った4基準すべてで同じである。
- 全方向モデルの実車evidence。検証は決定論的な単体検査と閉ループ回帰のみである
- 前進のみ設定（`limits.v_min = 0`）のAckermannで後方goalが与えられた場合の到達。BACは停止し、
  切り返しはNav2側のrecoveryに委ねる

実機の最終防護には、センサcoverageとtimeoutを独立に設定したCollision Monitor等を併用する。
